<h4 style='text-align: center;'><%= t("site.attempt") %></h4>
<%= form_for(@attempt) do |f| %>
  <% if @attempt.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@attempt.errors.count, "error") %> prohibited this attempt from being saved:</h2>

      <ul>
        <% @attempt.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :content, '内容' %>
    <%= f.text_area :content, class: 'form-control' %>
  </div>

  <div class="container">
    <div class="row">
      <%= f.submit "保存", class: 'btn btn-success col-xs-12' %>
    </div>
  </div>
<% end %>
